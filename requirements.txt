# requirements.txt
# ไลบรารีที่จำเป็นสำหรับรันเว็บแอป app.py
#
# หมายเหตุสำคัญ: เดิมเคยปักหมุดเวอร์ชันของ scikit-learn / Orange3 /
# Orange3-ImageAnalytics ไว้ตายตัว แต่พบว่าแต่ละแพ็กเกจมีข้อกำหนด
# (dependency) เกี่ยวโยงกันเองอยู่แล้ว (เช่น Orange3 บังคับช่วงของ
# scikit-learn และ orange-canvas-core, Orange3-ImageAnalytics ก็บังคับ
# ช่วงของ orange-canvas-core เช่นกัน) การปักหมุดตายตัวทำให้ pip/uv
# resolve dependency ไม่ได้บ่อยครั้ง ("No solution found...")
#
# จึงเปลี่ยนมาระบุแค่ "ขอบเขตล่าง" (>=) ของแพ็กเกจหลักที่แอปเรียกใช้ตรง ๆ
# แล้วปล่อยให้ pip/uv เลือกเวอร์ชันที่เข้ากันได้ทั้งหมดให้เองโดยอัตโนมัติ
# ซึ่งแม่นยำกว่าการเดาปักหมุดเองทีละตัว

streamlit>=1.38.0
joblib>=1.3.0

# โมเดล .pkcls ทั้ง 3 ไฟล์ถูกฝึกด้วยโปรแกรม Orange Data Mining จึงต้องใช้
# Orange3 ในการโหลดโมเดล (มิฉะนั้นจะเจอ ModuleNotFoundError: No module
# named 'Orange') และต้องใช้ Orange3-ImageAnalytics เพื่อแปลงภาพ X-ray
# ที่ผู้ใช้อัปโหลดให้เป็น embedding vector (SqueezeNet) แบบเดียวกับตอนฝึก
Orange3
Orange3-ImageAnalytics
Pillow>=10.0.0

# Orange3 เรียกใช้ pkg_resources (มากับ setuptools) ตอน import เพื่อจัดการ
# ระบบ add-on ของมัน แต่ setuptools ตั้งแต่เวอร์ชัน 82.0.0 เป็นต้นไป
# (ออกเมื่อ ก.พ. 2026) ได้ลบโมดูล pkg_resources ออกไปแล้วอย่างสมบูรณ์
# จึงต้องปักเพดานเวอร์ชันไว้ที่ <81 เพื่อให้ยังมี pkg_resources ใช้งานได้
# มิฉะนั้นจะเจอ ModuleNotFoundError: No module named 'pkg_resources'
setuptools<81

# Orange3-ImageAnalytics ผูกกับ Qt (ผ่านไลบรารี AnyQt) อยู่ภายใน แม้ตอน
# ใช้งานแบบ scripting (ไม่เปิดหน้าต่าง GUI) ก็ยังต้องมี Qt binding ติดตั้ง
# อยู่ในเครื่อง มิฉะนั้นจะเจอ "ImportError: PyQt4, PyQt5, PySide or PySide2
# are not available for import" จึงต้องติดตั้ง PyQt5 เพิ่ม (ดูไฟล์
# packages.txt คู่กันสำหรับ system libraries ที่ PyQt5 ต้องใช้ด้วย)
PyQt5
